import React from 'react';

function Logo(props){

    //const {width, height} = props;
    //const logo = require('../img/logo.jpg');

    return(
        <h1>X TOYS</h1>
        //<img width={width} height={height} src={logo} alt="Logo"></img>
    );
}

export default Logo;